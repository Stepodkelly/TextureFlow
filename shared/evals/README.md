# TextureFlow intelligence evals

`intelligence-cases.json` is the shared deterministic evaluation set for the
advisory intelligence package. It covers malicious notification content,
ambiguous person aliases, urgent requests, promotions, and cross-application
person context.

The fixtures use the canonical notification contract. Run them from the
`intelligence` directory with `npm test`.
