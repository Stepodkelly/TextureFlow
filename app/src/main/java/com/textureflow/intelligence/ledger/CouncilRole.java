package com.textureflow.intelligence.ledger;

/** Council role whose metrics are stored on a {@link RoleRunRecord}. */
public enum CouncilRole {
    TRIAGE,
    SUMMARIZER,
    DRAFTER,
    SKEPTIC
}
