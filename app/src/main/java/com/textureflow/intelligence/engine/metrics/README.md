# `intelligence/engine/metrics/` — Settings presenters (Stream L)

Pure Java copy and formatting for the Settings intelligence section. No `android.*` imports.

| File | Role |
|------|------|
| `AssessmentMetricsPresenter` | Last-N ledger lines: `level` + `reason_text` only |
| `ModelDownloadCopy` | ~529 MB size, Wi-Fi gate, progress strings |
| `PhoneBenchCopy` | Doc §9.4 tier words + missing-file / no-port copy |
| `PhoneBenchRunner` | Short generate via `ModelPort` / `ModelLifecycle`; never throws on `NoModelPort` |
